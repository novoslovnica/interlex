# Interlex Roadmap / Backlog

Приоритизированный список технического долга и возможных фич, составлен по итогам анализа проекта 2026-08-12. Источники: `AGENTS.md`, `ARCHITECTURE.md` ("Known Issues & Technical Debt"), прямой осмотр кода.

Статусы: ⬜ не начато · 🔄 в работе · ✅ сделано

---

## P0 — фундамент (дёшево, снижает риск всех остальных задач)

1. ✅ **Подключить vitest** (2026-08-12) — `vitest.config.mts`, `npm test`/`npm run test:run`. Сконвертированы `metrics.test.ts`, `morphology.test.ts` (все 20 assert → expect, все проходят) в реальные тесты; `declineNoun.test.ts` — в снапшот-тесты (старые `expectedForms` были праслав. реконструкциями, довирять их как "верные" нельзя без лингвиста, см. комментарий в файле). `analyze-text.test.ts`/`cqlParser.test.ts`/`tokenizer.test.ts` (без ассертов), `cqlTranslator.test.ts` (битый — левый импорт `@prisma/client`, несуществующий `analytics.db`), `CorpusInjector.test.ts` (пишет в живой `corpus.db`, сам `CorpusInjector` — мёртвый код) осознанно исключены из прогона, см. комментарии в `vitest.config.mts`. Попутно найдено и исправлено: `processors.ts` использовал `require()` внутри тела функций для уже статически импортированных модулей — не резолвилось под vitest/Vite, заменено на статические импорты (безопасно, те же модули уже импортировались в файле).
2. ✅ **CI на GitHub Actions** (2026-08-12) — `.github/workflows/ci.yml`: `test` (vitest) — блокирующий гейт, чистый; `lint` — запускается и виден, но `continue-on-error: true` (см. #12 — 205 реальных ошибок/116 warnings на момент внедрения, делать гейт блокирующим сейчас значило бы получить красный CI с первого дня); `build` — как есть, с текущим `ignoreBuildErrors: true`. Отдельный `tsc --noEmit` НЕ подключен: `tsconfig.json` сочетает `module: nodenext` + `moduleResolution: bundler` — комбинация, которую сам `tsc` CLI отвергает (TS5095/TS5109), хотя внутренний тайпчекер Next.js её принимает; обход через CLI-флаг даёт кучу ложных `Cannot find module` из-за другого алгоритма резолюции. De facto в проекте пока нет ни одного гейта типов — задача на будущее, не решена этим CI.
3. ✅ **`proxy.ts`** (2026-08-12, ранее назывался `middleware.ts` — в Next.js 16.2.9 конвенция переименована, см. `nextjs.org/docs/messages/middleware-to-proxy`) — грубая защита `/admin/**` и `/api/admin/**` по роли (ADMIN/MODERATOR) через `next-auth/jwt`'s `getToken` (без обращения к БД — `@/auth` тянет `PrismaAdapter`/`better-sqlite3`, несовместимые с Edge-рантаймом). Не заменяет точечные `checkPermission`/`requirePermission` в роутах. Проверено вживую: `/admin/words` без сессии редиректит, `/api/admin/*` возвращает `403 {"error":"Unauthorized"}`. Побочная находка: `redirect("/unauthorized")` в `lib/permissions.ts` ведёт на несуществующую страницу (сейчас 404) — уже было так до этой правки, не чинилось в рамках задачи.

## P1 — риски безопасности и целостности данных

4. ✅ **Nonce/replay-защита для Telegram-логина** (2026-08-12) — HMAC-хэш от Telegram уникален на каждое событие логина, поэтому сам служит nonce'ом: новая таблица `telegram_auth_nonces` (auth.db, миграция `scripts/db/2026-08-12-add-telegram-auth-nonce.ts`), claim через `INSERT OR IGNORE` (атомарно на уровне SQLite, устойчиво к параллельным запросам с одним payload'ом) в `lib/telegramAuthNonce.ts::claimTelegramAuthHash`, подключено в `auth.config.ts`'s `authorize()` между проверкой freshness (24ч) и возвратом пользователя. Попутная уборка строк вне 24ч окна — раз на каждый логин, отдельная DB-нагрузка незначительна при объёме проекта. Покрыто тестом с замоканным `@/lib/prisma` (без обращения к живой БД). Бэкап `auth.db.backup-before-telegram-nonce` перед применением DDL.
5. ⬜ **Разрешить Prisma migration drift на `interlex.db`** (`prisma migrate diff` + ручная baseline-миграция) — сейчас `migrate dev` предлагает full reset живой БД.
6. ✅ **`ų`/`u` кандидаты в `DbAnalyzer`** (2026-08-12) — лингвистический вердикт от мейнтейнера: обычная буква (`u`) — расширенное множество, покрывающее и себя, и упрощённое написание канонической (`ų`), но не наоборот; принцип общий для всех подобных пар букв ISV-орфографии, не только u/ų. Реализовано как отдельный `lib/corpus/tokenizer/spellingVariants.ts` (`expandSpellingVariants` — декартово произведение по расширяемым позициям слова, таблица `SIMPLIFIED_TO_CANONICAL` пока содержит только подтверждённую пару `u→['ų']`, расширять только по факту лингвистического подтверждения, не гадая). Подключено в `DbAnalyzer.analyzeWord`: варианты написания расширяются один раз в начале, дальше участвуют в поиске `inflection_anomalies`, генерации гипотетических баз (`generateHypotheticalBases`) и стем-префиксном фоллбеке (`findBestStemPrefixWord`) — везде объединяются в общий пул кандидатов, а не замещают точный поиск (тот же урок "не терять кандидата", что и в истории с `sųt`/`byti-VERB` vs `sut-AUX`, см. AGENTS.md). Продакшен-запрос (`createQueryWordsByBase` в `analyzer-factory.ts`) не потребовал изменений — просто получает более широкий список `bases`. Покрыто тестами (`spellingVariants.test.ts`, `dbAnalyzer.test.ts` с замоканным `queryWordsByBase`, без обращения к живой БД). Открыто: перепрогнать `scripts/db/2026-07-28-reanalyze-all-documents.ts` на реальном корпусе, чтобы фактически переразметить "sut"-вхождения (2325+) — не сделано в рамках этой сессии, отдельное решение (трогает продовые данные).
7. ✅ **Rate limiting на публичных API** (2026-08-12) — `lib/rateLimit.ts` (`RateLimiter`, in-memory fixed-window по IP — корректно для текущего деплоя, один долгоживущий `next start` процесс, без Redis; понадобится общее хранилище только если проект когда-нибудь станет многоинстансовым) + `getClientKey` (`x-forwarded-for`/`x-real-ip`, иначе общий bucket `"unknown"`). Подключено в `proxy.ts`: лимит 120 запросов/60с на клиента для всего `/api/**`, кроме `/api/auth/**` (не троттлится и не гейтится — часто дёргается при обычной загрузке страниц). Проверено вживую в браузере: 130 запросов подряд к `/api/lexicon` → 119 успешных + 11 с `429` и корректным `Retry-After`; `/api/admin/*` и обычный `/api/lexicon` не задеты регрессией. Покрыто юнит-тестами (`rateLimit.test.ts`, чистая логика без `NextRequest`).

## P2 — технический долг, повышающий вероятность будущих багов

8. ✅ **Унификация нейминга падежей** — сделано в два прохода.
   - *Первый проход* (2026-08-12): точечный фикс бага отображения. `CorpusTokenDisplay.tsx`/`TokenSidebar.tsx`'s `FEAT_LABELS` понимали только короткие коды (`nom`), а грамматический движок отдавал падеж полным словом (`nominative`). Добавлен `normalizeFeatValue(key, value)` в `CorpusTokenDisplay.tsx` (переиспользует `normalizeCaseValue` из `caseNormalize.ts`), экспортирован в `TokenSidebar.tsx`.
   - *Второй проход, тот же день, по прямому запросу мейнтейнера* — полная унификация в самом движке, не только на отображении. Оказалось, что расхождение не было чисто косметическим: комментарий в `lib/corpus/syntax/caseUtils.ts` (найден при расследовании) зафиксировал, что оно уже тихо ломало `PREPOSITION_GOVERNMENT`/`CASE_WEIGHTS` в скоринге дизамбигуации омонимов, и явно указывал, что переписывание движка — отдельная задача "по аналогии с миграцией ǫ→ų", требующая подтверждения мейнтейнера. Значения `Case` в `lib/grammar/endingsRegistry.ts` переведены с длинных слов на короткие UD-коды (те же, что уже использовал `GrammaticalCase`); переименованы ~210 ключей `SLAVIC_ENDINGS_REGISTRY` (единственный реестр с сырыми строковыми ключами — прилагательные/местоимения/числительные используют вычисляемые `[Case.NOMINATIVE]`, обновились автоматически); обновлены `grammemes.ts`'s `UD_CASE`/`CASE_FROM_UD` (сама БД не затронута — `buildGrammeme()` уже транслировал в короткий UD PascalCase перед записью, проверено на живых данных `ending_allophones`); переписаны все сравнения падежа в `declineNoun.ts`/`fourTonesGenerator.ts`/`encliticEngine.ts`; обновлены `reconstruct.ts`/`seed-endings.ts`/`run_validator.ts` и списки падежей на странице слова (`page.tsx`/`Word.tsx`/`NounDeclensionTables.tsx`, поле `lookup` для i18n сознательно оставлено длинным). Проверено: 105 из 105 значений в снапшот-тесте `declineNoun.test.ts` побайтово идентичны до/после (только имена ключей поменялись, ни одна словоформа не изменилась); число ошибок `tsc` упало со 174 до 163; вживую проверена таблица склонения на `/words/12` — все 7 падежей × 3 числа корректно заполнены. `caseNormalize.ts`/`caseUtils.ts` оставлены как страховка для уже сохранённых в `corpus.db` записей со старыми длинными кодами, не удалены.
9. ✅ **`base_homonyms` формат в `lib/dedup/mergeLexemes.ts`** (2026-08-12) — реальный баг был не в `app/admin/deduplication/actions.ts` (эта логика живёт в `lib/dedup/mergeLexemes.ts`, который `actions.ts` вызывает). Портирован `parseWordIds` из `scripts/db/2026-07-29-merge-preposition-duplicate-lexemes.ts` — понимает и `number[]`, и `{id,flavor}[]`. Тест на `parseWordIds`.
10. ✅ **N+1 на `/words/[id]`** (2026-08-12) — половина проблемы (запрос на каждую языковую таблицу) уже была решена раньше при консолидации 18 таблиц в единую `translations` (документация не обновлена, не значилось как ✅). Актуальным оставался только двойной вызов `getItem()` (`generateMetadata` + сама страница) — обёрнут в `React.cache`. Проверено вживую (страница слова рендерится, без ошибок в консоли).
11. ✅ **`matchCount=0` неоднозначность у пунктуации** (2026-08-12) — исправлено в `lib/corpus/tokenizer/tokenizer.ts` (основной путь — используется всеми crawler'ами, corpus-builder save и analyze) И отдельно в `lib/corpus/reanalyzeDocument.ts` (независимая дублирующая ветка, найдена при проверке всех реальных call site'ов, а не только документированного). Пунктуация теперь получает `matchCount: 1` (детерминированное, не омонимичное) вместо неявного `0` (зарезервировано за "не найдено в словаре"). Существующий защитный фильтр `wordIndex: { not: -1 }` в `generateCorpusCandidateProposals` не тронут — избыточен, но безвреден. Тест на `Tokenizer.tokenizeSentence`.
12. ⬜ Сократить `any` (100+ мест) — постепенно, попутно с другими задачами.
13. ⬜ AuditLog для `auth.db`/`corpus.db`/`library.db` — сейчас только `interlex.db`.

## P3 — фичи по теме проекта

14. ⬜ Граф/облако синонимов на странице слова (`SemanticRelation` + черновой `SynonymGraph.tsx` уже есть).
15. ⬜ Примеры из корпуса на странице слова (`CorpusToken.wordSlug` уже линкует к лексеме).
16. ⬜ Модераторская UI для `VerbGovernment`/предложного управления — разблокирует простаивающий Pass C дизамбигуации.
17. ⬜ Флэшкарты/повторение по CEFR и частотности (`cefrLevel`/`corpusFrequencyPerMln` уже посчитаны).
18. ⬜ Fuzzy-matching для RuWordNet (`lib/levenshtein.ts`) — поднимет текущие ~52% сопоставленных значений.

## P4 — крупные, долгие инициативы

19. ⬜ Рекурсивная обработка вложенных придаточных в syntax-парсере (`complexSentence.ts`).
20. ⬜ Векторные embeddings для семантического поиска/похожих слов.
21. ⬜ Публичный read-only API для сторонних интеграций.
22. ⬜ Расследовать утечку памяти Prisma в `$transaction` — перепроверить на будущих версиях Prisma перед новыми bulk-скриптами.

---

## Лог выполнения

- 2026-08-12: roadmap создан; P0 закрыт целиком (vitest, CI, proxy.ts). Новые находки по пути: `/unauthorized` страницы не существует (лежит в основе #3); tsconfig.json несовместим с прямым `tsc` CLI; реальный lint-долг вне мёртвого worktree-каталога — 205 ошибок / 116 warnings (уточняет масштаб #12).
- 2026-08-12: устранено дублирование `MorphoGrammarFeats` (два независимых объявления с одним именем — `lib/grammar/morphology/index.ts` и `lib/grammar/common/feats.ts`, оба занесены одним коммитом "inject corpus" 2026-07-03, не связаны). Оставлена версия с типизацией через `GrammaticalCase`/`GrammaticalNumber`/`GrammaticalGender`; `morphology/index.ts` теперь импортирует и реэкспортирует её. По пути найдены и исправлены два реальных бага: (1) `processNumeral` в `processors.ts` отдавал `feats.number` длинной формой (`'singular'`) вместо короткой — CoNLL-U экспорт молча терял Number-фичу для числительных; (2) `feats.gender`/`Lexeme.gender` на отображении в `TokenSidebar.tsx` сравнивались со строчными `'masc'` при том что и движок (после миграции), и БД (после миграции 2026-07-26) уже отдают заглавную UD-форму `'Masc'`. `gender` во всём `processors.ts` переведён на заглавную UD-форму (matching `GrammaticalGender`); `conllu.ts`'s `mapGender`/`mapCase`/`mapNumber` не потребовали изменений — уже регистронезависимы. `caseNormalize.ts`/`caseUtils.ts` и новая gender-нормализация в `normalizeFeatValue` оставлены как страховка для уже сохранённых в `corpus.db` записей со старой формой. tsc error count: 156 (ниже исходного базлайна 174, ниже промежуточного 157).
